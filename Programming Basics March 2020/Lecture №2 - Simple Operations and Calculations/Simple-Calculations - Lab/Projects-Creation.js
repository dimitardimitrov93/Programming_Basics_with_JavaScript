function projectCompletion(name, projectNum) {
    let timeForCompletion = projectNum * 3;
    console.log(`The architect ${name} will need ${timeForCompletion} hours to complete ${projectNum} project/s.`);
    
}